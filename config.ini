[hardware]
  dev=/dev/nvme0n1 # device (be careful!)
  efi=1G # size of efi partition
  swap=4G # size of swap memory
  cpu=intel # 'intel' or 'amd'

[network]
  wifi=my-wifi
  wifipw=password

[system]
  hostname=arch
  tz=UTC
  locale=en_US

[sudo user]
  user=my-name
  userpw=password

[options]
  overwrite=false # 'true' or 'false'
